pipeline {
    agent any

    stages {
        stage('Build docker image') {
            steps {
                echo "hi this is build"
            }
        }
        stage('Run the image'){
            steps{
                echo "hi this is run"
            }
        }
        
    }
}